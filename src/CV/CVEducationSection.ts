import CVListSection from "./CVListSection.ts";

class CVEducationSection extends CVListSection {
    constructor() {
        super({
            "SECTION_NAME": "EDUCATION"
        });
    }
}

export default CVEducationSection;